# Local smoke-test values only. Replace every value in Coolify secret variables.
DB_NAME=limesurvey
DB_USER=limesurvey
DB_PASSWORD=example-only-not-a-secret
DB_ROOT_PASSWORD=example-root-only-not-a-secret
